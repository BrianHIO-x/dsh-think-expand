# Bundle patch for `dsh plugin --profile <name> add dsh-think-expand`.
# Inserts the host row; the browser half is discovered from package.json
# `dsh.client` + exports["./client"].
- insert:
    - id: think-expand
      name: dsh-think-expand
