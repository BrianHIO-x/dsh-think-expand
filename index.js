/**
 * Host half. This plugin is a Web UI surface: the empty apply exists so the
 * row appears in the Loader. The browser half ships via exports["./client"].
 */
export function apply() {
  console.log('[dsh-think-expand] plugin loaded!')
}
